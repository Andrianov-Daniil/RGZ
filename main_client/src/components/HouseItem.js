import React from "react";
import { Col } from "react-bootstrap";

const HouseItem = ({house}) => {
    return(
        <Col md={3}>
            item
        </Col>
    )
}

export default HouseItem;